---
import BaseLayout from '@/layouts/BaseLayout.astro';

const timeline = [
  { year: '2020', title: 'El nacimiento', text: 'Se abre el primer DAK Burger en La Cala de Mijas y nace THE WALL, uno de los retos más conocidos de España.', color: 'primary' },
  { year: '2021', title: 'Crecimiento', text: 'Abrimos en Málaga centro y consolidamos la marca con mayor presencia en redes y retos virales.', color: 'accent' },
  { year: '2022', title: 'Evolución', text: 'Reformamos el local principal, ampliamos aforo y lanzamos WALLY como nuevo desafío gastronómico.', color: 'primary' },
  { year: '2023', title: 'Expansión', text: 'Nace B-Smash y comenzamos la etapa de eventos con food truck en The Champions Burger.', color: 'accent' },
  { year: '2024', title: 'Reconocimiento', text: 'Mejor burger de Malaga · Segunda de Europa · Tercera de Espana.', color: 'primary' }
];
---

<BaseLayout title="Conócenos | DAK Burger">
  <!-- Hero -->
  <section class="relative flex h-[50vh] min-h-[400px] items-center justify-center overflow-hidden pt-20">
    <img src="/gallery/burger2.jpg" alt="DAK Burger" class="absolute inset-0 h-full w-full object-cover" />
    <div class="absolute inset-0 bg-gradient-to-b from-dark/70 via-dark/50 to-dark/80"></div>
    <div class="relative z-10 text-center px-4">
      <p class="text-sm font-bold uppercase tracking-[0.3em] text-accent">Nuestra historia</p>
      <h1 class="mt-3 font-heading text-5xl text-white md:text-7xl">FROM MALAGA<br/>TO THE WORLD</h1>
    </div>
  </section>

  <!-- Intro -->
  <section class="page-container max-w-3xl py-16 text-center">
    <p class="text-lg leading-8 text-slate-600 md:text-xl">
      Nacimos en 2020 en plena pandemia con un objetivo claro: hacer las burgers que siempre habíamos querido comer.
      Nuestro fundador, <strong class="text-slate-800">Domingo González</strong>, abrió un pequeño local en La Cala de Mijas sin imaginar hasta dónde llegaríamos.
    </p>
  </section>

  <!-- Timeline -->
  <section class="page-container max-w-4xl pb-20">
    <h2 class="mb-12 text-center font-heading text-4xl text-primary md:text-5xl">Nuestra historia</h2>

    <div class="relative">
      <!-- Vertical line -->
      <div class="absolute left-4 top-0 bottom-0 w-0.5 bg-slate-200 md:left-1/2 md:-translate-x-0.5"></div>

      {timeline.map((item, idx) => (
        <div class={`relative mb-12 flex flex-col md:flex-row ${idx % 2 === 0 ? 'md:flex-row' : 'md:flex-row-reverse'}`}>
          <!-- Dot -->
          <div class={`absolute left-4 top-1 z-10 h-4 w-4 -translate-x-1/2 rounded-full border-4 border-white md:left-1/2 ${item.color === 'accent' ? 'bg-accent' : 'bg-primary'}`}></div>

          <!-- Content -->
          <div class={`ml-10 md:ml-0 md:w-1/2 ${idx % 2 === 0 ? 'md:pr-12 md:text-right' : 'md:pl-12'}`}>
            <div class="card p-6">
              <span class={`font-heading text-3xl ${item.color === 'accent' ? 'text-accent' : 'text-primary'}`}>{item.year}</span>
              <h3 class="mt-1 text-xl font-bold text-slate-800">{item.title}</h3>
              <p class="mt-2 text-sm leading-relaxed text-slate-600">{item.text}</p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- Closing CTA -->
  <section class="bg-slate-50 py-16 text-center">
    <div class="page-container">
      <p class="text-lg text-slate-600">Esto es solo el comienzo.</p>
      <h2 class="mt-2 font-heading text-4xl text-primary md:text-5xl">Seguimos haciendo historia</h2>
      <div class="mt-8 flex flex-wrap items-center justify-center gap-4">
        <a href="/reserva" class="btn-primary">Reservar mesa</a>
        <a href="/carta" class="btn-outline">Ver el menu</a>
      </div>
    </div>
  </section>
</BaseLayout>
